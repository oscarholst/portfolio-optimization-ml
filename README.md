# OMXS30 Stock Selection via XGBoost

This project implements a machine learning framework that selects the top 10 performing stocks from the OMXS30 index each month. Selections are based on relative return predictions generated by an portfolio ranking XGBoost model.

## Performance Metrics (Jan 2023 - Jan 2026)

| Metric | Strategy Portfolio (Top 10 Picks) | Benchmark (OMXS30) | Excess (Alpha) |
| :--- | :--- | :--- | :--- |
| Total Return | 51.37% | 31.13% | +20.24% |
| Sharpe Ratio | 0.67 | - | - |

## Visualizations

* Portfolio Performance: `images/portfolio_chart.png` (Cumulative return vs OMXS30)
* Alpha Generation: `images/alpha_chart.png` (Cumulative excess return)
* Stress Test: `images/noise_test_chart.png` (Noise test baseline)

## Methodology and Validation

The framework uses specific validation methods to ensure the backtest operates realistically without look-ahead bias:

* TimeSeriesSplit: The model is trained strictly on historical data and tested on a rolling, chronological basis to mimic live trading.
* Fold-Local Scaling: Feature scaling is applied strictly within each training fold, ensuring no future information contaminates the model during preprocessing.
* Noise Test Control: A separate control test replaces all model predictions with random noise. The resulting Alpha collapsed to 4.92%, confirming that the backtest structure is sound and the actual model's 20.24% Alpha is driven by real predictive signals.

## Project Structure

* `notebooks/01_data_exploration.ipynb`: Feature engineering, fold-local scaling, and XGBoost training.
* `notebooks/02_portfolio_optimization.ipynb`: Monthly top 10 stock selection and portfolio simulation.
* `notebooks/03_look_ahead_tests.ipynb`: Look-ahead bias stress tests (Noise Test).
* `requirements.txt`: Environment dependencies.
* `presentation.pdf`: Project summary slide deck.